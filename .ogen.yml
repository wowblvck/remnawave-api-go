parser:
  # Enable type inference from schema properties
  infer_types: true
  # Allow resolving remote references
  allow_remote: true

generator:
  ignore_not_implemented: ["object defaults", "array defaults", "allOf with oneOf"]
  features:
    disable_all: true
    enable:
      # Core features
      - "paths/client"
      - "client/request/validation"
      - "client/request/options"
      - "client/editors"

      # Observability & Instrumentation
      - "ogen/otel"

      # Code quality improvements
      - "ogen/unimplemented"
      - "debug/example_tests"
